package com.gu.state

import com.typesafe.scalalogging.LazyLogging
import org.scalatest.{FlatSpec, Matchers}

class StateClassesSpec extends FlatSpec with Matchers with LazyLogging {

  "State" should "serialise correctly" in {
    //val state = CreatePaymentMethodState(UUID.randomUUID(), User(""))
    true should be(true)
  }

}
